language: cpp

before_install:
  - sudo do-release-upgrade
  - sudo apt-get update -qq
  - sudo apt-get install -y libkdegames-dev

compiler:
  - gcc

before_script:
  - mkdir build
  - cd build
  - cmake ..

script: make

